import app from 'Routes/app'

let routeFiles = []

//importing routes from : routes folder

const routes =  routeFiles.concat(
    //adding the imported route files to the routes object
    app

)
export default routes
